{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug index.html (archivo)",
      "type": "chrome",          
      "request": "launch",
      "file": "${workspaceFolder}/index.html",   
      "webRoot": "${workspaceFolder}"
    }
  ]
}